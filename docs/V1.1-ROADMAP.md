# AIUsageBar next version (after v1.0.0)

**Production baseline:** `0cf9989e8389d42a9d2290e5eb8daa765da496d1`  
**Release:** v1.0.0, notarized Developer ID, tag `v1.0.0`  
**Do not** treat the Cursor V2 pipeline freeze (`194225f` / PR #27) as this baseline.

## This slice (v1.1 language)

Traditional Chinese and English UI, with an in-app picker:

- Follow system (`zh` / `zh-*` → 繁體中文, otherwise English)
- 繁體中文
- English

Strings live in `AIUsageBar/Localization/L10n.swift` (no String Catalog; Linux/Xcode-less agents can edit it). Changing the picker refreshes usage so stored reset/error copy matches the new language.

ChatGPT / Claude / Grok cards stay. No Cursor or Gemini menu cards in this slice.

## Later slices (not this PR)

| Slice | Scope |
|---|---|
| Cursor provider | Menu card only after a live usage source PASSes. Research-only local DB stays HOLD. |
| Gemini provider | Apps vs CLI/Code Assist are separate meters unless proven identical. No card until a source PASSes. |
| Other providers | Copilot remains architecture benchmark only unless separately authorized. |

## Isolation

New production work branches from `main` at `0cf9989…`. Do not merge the frozen Cursor pipeline candidate to `main` as part of this line.
